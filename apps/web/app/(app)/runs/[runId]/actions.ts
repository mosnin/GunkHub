'use server'

// Re-export from the lib actions module so this file serves as the
// co-located server action entry point for the route segment.
export { updateRunTagsAction } from '@/lib/actions/runs'
