{
  "name": "@agent-flight-recorder/tests",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "e2e": "playwright test --config=../playwright.config.ts",
    "e2e:ui": "playwright test --config=../playwright.config.ts --ui",
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.convex-seam.json",
    "lint": "eslint . --ext .ts,.tsx --max-warnings 0"
  },
  "dependencies": {
    "@agent-flight-recorder/contracts": "workspace:*",
    "@agent-flight-recorder/sdk": "workspace:*",
    "@agent-flight-recorder/cli": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0",
    "@types/node": "^20.0.0",
    "@playwright/test": "1.56.1"
  }
}
